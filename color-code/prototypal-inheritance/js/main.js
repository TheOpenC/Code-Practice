class Person {
    talk() {
        return 'talking';
    } 
}
const me = new Person();
const you = new Person();

//its useful to use constructors to add properties and then use inheritance for methods via the prototype. 


